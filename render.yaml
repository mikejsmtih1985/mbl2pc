services:
  - type: web
    name: mbl2pc-fastapi
    env: python
    buildCommand: "pip install -r requirements.txt"
    startCommand: "uvicorn src.mbl2pc.main:app --host 0.0.0.0 --port 10000"
    plan: free
    envVars:
      - key: PORT
        value: 10000
